name: mofsynth_adv_env
channels:
  - conda-forge
  - defaults
dependencies:
  - python>=3.10
  - tblite
  - tblite-python
  - pip
  - pip:
      - -e .

# - mofsynth_adv for pypi install
